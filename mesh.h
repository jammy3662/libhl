#pragma once
/*
struct Mesh
{
	float* vertices;
	uint vertexCount;
	float* normals;
	float* texCoords;
	float* lightCoords;
	uint8* colors;
	uint8* indices;
	uint indexCount;
	
	void loadFile(char* path)
	{
		Assimp::Importer importer;
		aiScene* scene = importer.ReadFile(path, aiProcess_Triangulate | aiProcess_FlipUVs);
		for (int i = 0; i < scene->mNumMeshes; i++)
		{
			scene->mMeshes[i]->mNumVertices
		}
	}
};

// collection of meshes and materials
struct Model
{
	
};
*/